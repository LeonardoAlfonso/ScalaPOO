import org.scalatest.FunSuite
import org.scalatest.BeforeAndAfter
import Banco._

class UnitariasBanco extends FunSuite with BeforeAndAfter 
{
    //var banco : Banco = new Banco

    test("Verificar que se apruebe un cheque si tiene saldo suficiente y la central de riesgo contesta correctamente") (pending)
    
    test("Verificar que se construye un cheque si se tiene un código de aprobación correcto") (pending)
}